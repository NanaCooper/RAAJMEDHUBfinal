// This plugin is intentionally a no-op.
// Its original functionality (fixing non-modular include errors and deployment targets)
// is now handled comprehensively by withFirebaseAuthSwiftHeaderFix.js to avoid conflicts.
const withFirebaseNewArchFix = (config) => config;

module.exports = withFirebaseNewArchFix;
